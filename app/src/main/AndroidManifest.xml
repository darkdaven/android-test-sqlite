<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itla.school">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name="com.itla.school.activity.CreateSubjectActivity" />
        <activity android:name="com.itla.school.activity.CreateCareerActivity" />
        <activity android:name="com.itla.school.activity.CareerListActivity" />
        <activity android:name="com.itla.school.activity.StudentListActivity" />
        <activity android:name="com.itla.school.activity.CreateStudentActivity" />
        <activity android:name="com.itla.school.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>